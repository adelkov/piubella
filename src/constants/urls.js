const base = "https://piubella-79e1b.firebaseio.com/"

export const products = base + 'products'
export const users = base + 'users'
export const carts = base + 'carts'
export const orders = base + 'orders'

